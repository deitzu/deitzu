# Profile README — Cool-but-Professional Enhancement Plan

## Context
The profile README (`deitzu/deitzu`) was already redesigned to a clean dark+teal minimal layout and is live on `main`. The user now wants it **cooler but still professional**. Verified-reliable widgets were researched (many popular services are dead: `github-readme-stats.vercel.app` is permanently paused; `github-profile-summary-cards` 404; `streak-stats.deno.dev` 404). Chosen additions, all confirmed returning HTTP 200 in testing:

- `readme-typing-svg.demolab.com` — animated typing header (maintained, 200).
- Contribution **snake** via GitHub Action `Platane/snk` — self-generated SVG committed to the repo, so it never depends on a flaky third party (200-capable, reliable).
- `shields.io` — static badges, always up, no rate limits (200).

**Security note (carried over):** the user's PAT was previously leaked into the repo and is now **revoked**. No tokens/secrets may be placed in `README.md`. The verified-working `github-stats-extended.vercel.app` stats cards need no token.

## Goal
Add three elements to the existing dark+teal README without breaking the current working widgets (stats, trophies, visitor counter, skillicons, featured project).

## Teal palette (keep consistent)
`bg 0d1117`, `text c9d1d9`, `teal 2dd4bf`, `teal-light 5eead4`.

## New README structure (top → bottom)
1. **Animated typing header** (replaces the static `# Hi, I'm Deitzu` heading) + the existing visitor-counter badge below it.
2. **Currently Learning** — shields.io `for-the-badge` row: C#, Java, JavaScript (teal).
3. **Tech Stack & Tools** — keep existing `skillicons.dev` block.
4. **Interests & Learning** — keep existing short bullets.
5. **Featured Project** — keep compact Darynxia block.
6. **Contribution Snake** — embedded dark SVG generated by the Action.
7. **Stats** — keep `github-stats-extended` profile + top-langs cards.
8. **Trophies** — keep `github-readme-trophy` row.
9. **Connect** — shields.io row: GitHub (links to `https://github.com/deitzu`) + Email (placeholder `mailto`, user fills in).

## Exact snippets (verified-working params)

**Typing header** (teal, centered, multi-line):
```md
<p align="center">
  <img src="https://readme-typing-svg.demolab.com/?lines=Deitzu;Game+Server+Hobbyist;Learning+C%23+%2B+Java+%2B+JavaScript;Builder+of+Darynxia&center=true&width=600&height=60&color=2dd4bf&font=Fira%20Code&pause=1000&size=24" alt="Typing SVG" />
</p>
<p align="center">
  <img src="https://komarev.com/ghpvc/?username=deitzu&label=Profile%20Views&color=2dd4bf&style=flat-square" alt="Profile Views" />
</p>
```

**Currently Learning (shields):**
```md
<p align="center">
  <img alt="C#" src="https://img.shields.io/badge/C%23-2dd4bf?style=for-the-badge&logo=csharp&logoColor=white" />
  <img alt="Java" src="https://img.shields.io/badge/Java-2dd4bf?style=for-the-badge&logo=openjdk&logoColor=white" />
  <img alt="JavaScript" src="https://img.shields.io/badge/JavaScript-2dd4bf?style=for-the-badge&logo=javascript&logoColor=white" />
</p>
```

**Contribution snake (embed):**
```md
<p align="center">
  <img src="https://raw.githubusercontent.com/deitzu/deitzu/main/github-contribution-grid-snake-dark.svg" alt="Contribution Snake" width="100%" />
</p>
```

**Connect (shields):**
```md
<p align="center">
  <a href="https://github.com/deitzu"><img alt="GitHub" src="https://img.shields.io/badge/GitHub-deitzu-2dd4bf?style=for-the-badge&logo=github&logoColor=white" /></a>
  <a href="mailto:you@example.com"><img alt="Email" src="https://img.shields.io/badge/Email-contact%40example.com-2dd4bf?style=for-the-badge&logo=gmail&logoColor=white" /></a>
</p>
```
(Replace `you@example.com` with the real address, or drop the Email badge if unwanted.)

## GitHub Action — `.github/workflows/snake.yml`
Generates the snake SVGs and commits them to `main` (profile branch). Requires `permissions: contents: write`.
```yaml
name: Generate Snake
on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Platane/snk@v3
        with:
          github_user_name: deitzu
          outputs: |
            github-contribution-grid-snake.svg?palette=github-dark
            github-contribution-grid-snake-dark.svg?palette=github-dark
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: update contribution snake" || echo "no changes"
          git push
```
Note: `palette=github-dark` yields a dark background with a teal/green snake — on-theme. If a stricter teal snake is wanted, add `&color_snake=%232dd4bf` (encode `#` as `%23`).

## Implementation steps
1. Edit `README.md`: insert typing header (replace static `# Hi, I'm Deitzu` + keep the one-line role as a subtitle if desired), add "Currently Learning" shields row after the header, add the snake embed before/after Stats, add the "Connect" shields row at the end. Keep all currently-working widgets.
2. Add `.github/workflows/snake.yml` with the snippet above.
3. Commit to `main` (local branch). **Keep the kilo `prepare-commit-msg` hook disabled / `co-author-trailer` emptied** so no `kiloconnect[bot]` trailer is re-added (the earlier PAT leak came from a web edit — avoid repeating).
4. Push `main` to `origin` (normal push; if history diverged, reconcile, then force-with-lease only if necessary).
5. The snake SVG appears after the first workflow run (manual "Run workflow" or next push/cron).

## Validation
- Open `https://github.com/deitzu` and confirm:
  - Typing header animates and is teal.
  - "Currently Learning" and "Connect" shields load.
  - Snake SVG renders (after first Action run); raw URL returns an SVG.
  - Existing stats (`github-stats-extended`), trophies, visitor counter, skillicons still load.
- Confirm the pushed `README.md` contains **no** `github_pat`, `token=`, or `github-readme-stats.vercel.app`.

## Risks / open items
- The snake Action pushes to `main`; ensure no branch-protection rule blocks it (this repo has none). It adds a small automated commit on each run — expected.
- `readme-typing-svg.demolab.com` is a third-party host; if it ever goes down, only the header is affected (graceful degradation).
- The kilo co-author hook may be re-created by the environment on future local commits; if a `Co-authored-by: kiloconnect[bot]` trailer reappears, disable the hook again and amend it out before pushing.
- Email badge uses a placeholder — user must supply a real address or remove it.
